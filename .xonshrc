aliases['e'] = 'emacs'
aliases['g'] = 'git'
$PATH.append('%s/bin' % $HOME)

import os.path
if not os.path.isfile('%s/private/.ssh/git' % $HOME):
    ~/bin/mnt

/usr/bin/keychain $HOME/.ssh/git

# workaround for https://github.com/xonsh/xonsh/issues/2603
$XONSH_APPEND_NEWLINE=True